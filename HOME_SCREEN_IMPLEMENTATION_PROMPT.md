# 防災アプリ ホーム画面再現用プロンプト

以下の指示を、別のシステムを実装する AI に一度だけ渡してください。既存システムの技術構成に合わせて実装方法は置き換えてよいですが、画面の機能、情報構成、配色、文字サイズ、ボタンサイズ、レスポンシブ挙動、日英対応は省略しないでください。

---

## 依頼

防災アプリの「ホーム画面」を、以下の仕様どおりに実装してください。

既存の他画面や既存機能を壊さず、ホーム画面に必要な共通ヘッダー連携、避難所データ取得、災害報告 API、気象警報 API、指示・発信データ取得だけを接続してください。

実装後は、デスクトップとスマートフォンの両方で表示確認を行い、機能と見た目がこの仕様を満たす状態まで修正してください。

このプロンプトを受け取った AI は、まず対象プロジェクトの既存構成を確認し、そのプロジェクトのフレームワーク・テンプレート・CSS・API を再利用してください。質問で止まらず、既存の命名規則に合わせて実装、検証、必要な修正まで行ってください。ホーム画面以外を不要に作り直したり、既存データを削除したりしないでください。

---

## 1. ホーム画面の全体構成

画面上部から次の順序で構成してください。

1. 共通ヘッダー
2. ホーム画面内の目次
3. メインコンテンツ
   - 避難所マップ
   - 災害情報報告フォーム
   - 気象警報・注意報
   - 指示・発信ボード

デスクトップでは、メインコンテンツを次の 2 カラム構成にしてください。

- 左側：避難所マップ、気象警報・注意報、指示・発信ボード
- 右側：災害情報報告フォーム

CSS Grid の概念としては次の配置です。

```text
map      report
weather  report
notice   report
```

- メインコンテンツの最大幅：`910px`
- 画面左右に最低 `16px` 程度の余白
- デスクトップのカラム間隔：`28px`
- カード間隔：`36px` 程度

スマートフォンでは 1 カラムにし、次の順番で縦に並べてください。

1. 避難所マップ
2. 気象警報・注意報
3. 指示・発信ボード
4. 災害情報報告フォーム

---

## 2. 共通ヘッダー

ホーム画面から別画面へ移動しても、共通ヘッダーの言語選択状態を維持してください。

ヘッダーには次のリンクを表示します。

- ホーム
- 避難所検索
- 避難所登録
- 指示・発信
- ログインまたはログアウト
- 言語選択

言語選択はプルダウンとし、次の 2 言語に対応してください。

- 日本語
- English

プルダウンの変更時に、選択した言語をサーバー側のセッションへ保存してください。

必要な条件：

- セッションの初期値は日本語
- 不正な言語値は日本語へ戻す
- 言語変更後は変更前の画面へ戻る
- ホーム、避難所検索、検索結果、避難所登録、指示・発信ボードの全画面で選択状態を維持
- HTML の `lang` 属性も `ja` または `en` に変更
- ホーム画面の見出し、ボタン、フォーム、地図ポップアップ、エラー表示を選択言語に合わせる

既存システムに共通テンプレートがある場合は、言語選択部分だけ共通テンプレートへ追加してください。既存のナビゲーション配置や機能は変更しないでください。

ホーム画面では、共通ヘッダーの既存ナビゲーションを表示したままにしてください。ヘッダーの仕様は次のとおりです。

- 高さ：デスクトップ `70px`、スマートフォンは `min-height: 70px` で内容に応じて拡張
- 左右 padding：デスクトップは最低 `24px`、最大コンテンツ幅に合わせて調整
- 背景：`#2f5baa`
- 文字色：`#ffffff`
- アプリ名：デスクトップ `25px`、スマートフォン `21px`、太字
- ナビゲーション間隔：`12px`、スマートフォン `8px`
- スマートフォンのナビゲーションは横スクロール可能にする
- ナビゲーションの文字サイズ：デスクトップ `14px`、スマートフォン `12px`
- ナビゲーションボタンの角丸：`6px`
- ナビゲーションボタンの padding：上下 `8px`、左右 `12px`
- ホーム：`#2f5baa`
- 避難所検索：`#239b4b`
- 避難所登録：`#d88421`
- 指示・発信：`#7651a8`
- ログイン：`#435466`
- ログアウト：`#bd4b48`
- ナビゲーションボタンの文字色：`#ffffff`、太字、下線なし

Font Awesome 6.5.2 を使える場合は、次の CDN を読み込み、既存画面と同じアイコンを使用してください。

```html
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
```

使用する主なアイコンは、位置情報 `fa-location-dot`、現在地 `fa-location-crosshairs`、展開 `fa-expand`、災害 `fa-triangle-exclamation`、気象 `fa-cloud-sun`、更新 `fa-arrows-rotate`、発信 `fa-bullhorn`、シールド `fa-shield-halved`、送信 `fa-paper-plane` です。

---

## 3. デザインと配色

ホーム画面では次のデザインを使用してください。

### フォント

Google Fonts または既存のフォント読み込み機能で `Noto Sans JP` を使用してください。

```css
font-family: 'Noto Sans JP', sans-serif;
```

### 色

```css
--home-blue: #2f5baa;
--home-green: #239b4b;
```

用途別の色は次のとおりです。

- メインヘッダー：`#2f5baa`
- 主見出し：`#2f5baa`
- 主ボタン：`#2f5baa`
- 成功・問題なし・空き：`#239b4b`
- 補助テキスト：`#506174` または `#607286`
- 濃い本文・ラベル：`#435466`
- カード背景：`#ffffff`
- 補助パネル背景：`#f7f8fa`
- 補助情報背景：`#eaf2fb`
- 入力欄の枠線：`#b8c2ce`
- エラー・危険・混雑：`#c0392b`
- やや混雑：`#d99a16`
- 薄い枠線：`#f0f2f6`

背景は白を基本にし、カードには薄い枠線と控えめな影を付けてください。

```css
border: 1px solid #f0f2f6;
box-shadow: 0 5px 18px rgba(41, 66, 102, 0.08);
```

カードの角丸はデスクトップでは `17px` 程度、地図や補助パネルでは `11px` 程度にしてください。スマートフォンではカードを画面幅いっぱいにし、左右の角丸と左右の枠線をなくしてください。

---

## 4. 文字サイズとタップ領域

次の文字サイズを基準にしてください。

- ヘッダーのアプリ名：`25px`、太字
- スマートフォンのアプリ名：`21px`
- セクション見出し：`23px`、太字
- スマートフォンのセクション見出し：`19px`
- 目次見出し：`19px`
- 目次リンク：`15px`、太字
- 通常本文：`15〜16px`
- フォームのラベル：`16px`、太字
- 入力欄・選択欄：`16px`
- 地図操作ボタン：`15px`、太字
- 地図の凡例：`14px`
- 地図ポップアップ：`16px`、行間 `1.7`
- 補足情報：`13〜14px`

目次リンクと地図操作ボタンは、スマートフォンで押しやすいように高さを最低 `44px` にしてください。

フォームの入力欄も、スマートフォンで操作しやすいように上下 `13px` 程度の padding を設けてください。

---

## 5. ホーム画面内の目次

ヘッダー直下、メインコンテンツより上に目次を表示してください。既存の `nav` 非表示 CSS などによって目次が消えないようにしてください。

目次の項目は次の 4 つです。

- 避難所マップ
- 災害の情報を報告する
- 気象警報・注意報
- 指示・発信ボード

各項目は同一ページ内のアンカーリンクにしてください。

```text
#shelter-map
#disaster-report
#weather-warnings
#resident-notices
```

目次の仕様：

- 背景：`#f7f8fa`
- 枠線：`#f0f2f6`
- 角丸：`11px`
- padding：上下 `18px`、左右 `22px`
- リンクは白背景
- リンク文字は `#2f5baa`
- hover/focus 時の背景は `#e4edf8`
- 目次リンクの高さは最低 `44px`
- スマートフォンでは横スクロール可能にする
- アンカー移動後に見出しが画面上端に隠れないよう `scroll-margin-top: 16px` 程度を設定

---

## 6. 避難所マップ

### 地図

Leaflet と OpenStreetMap タイルを使用してください。

```html
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css">
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
```

```javascript
L.map('shelterMap').setView([40.8221, 140.7474], 12);
L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
  attribution: '© OpenStreetMap contributors'
});
```

地図サイズ：

- デスクトップ：高さ `364px`
- スマートフォン：高さ `340px`
- overflow：`hidden`
- 角丸：`11px`

### 避難所データ

サーバーから次の形式の避難所配列を受け取ってください。

```json
[
  {
    "id": 1,
    "name": "避難所名",
    "address": "住所",
    "accepting": "受け入れ可",
    "congestion": "空き",
    "latitude": 40.8221,
    "longitude": 140.7474
  }
]
```

緯度または経度がないデータはマーカーを作成せず、安全にスキップしてください。

### マーカーの状態色

状態に応じてマーカーを色分けしてください。

- `accepting` が `受け入れ可` かつ `congestion` が `空き`：緑 `#239b4b`
- `congestion` が `やや混雑`：黄色 `#d99a16`
- `congestion` が `混雑`：赤 `#c0392b`
- `accepting` が `受け入れ不可`：赤 `#c0392b`

色だけに頼らず、必ず凡例とポップアップの文字でも状態を表示してください。

マーカーは `L.divIcon` などで作成し、次の大きさを目安にしてください。

- マーカー：`24px × 24px`
- 白い枠線：`3px`
- 丸形
- 影付き

### ポップアップ

マーカーをクリックしたら次の情報を表示してください。

- 避難所名
- 住所
- 受け入れ可否
- 混雑度

ポップアップは次の文字サイズにしてください。

- `font-size: 16px`
- `line-height: 1.7`
- margin：上下 `14px`、左右 `16px` 程度

ユーザー入力値を HTML に直接挿入する場合は、必ず HTML エスケープしてください。少なくとも `& < > ' "` をエスケープしてください。

### 地図の初期表示

座標を持つ避難所が 1 件以上ある場合、全マーカーが収まるように `fitBounds` を実行してください。

```javascript
map.fitBounds(bounds, {
  padding: [24, 24],
  maxZoom: 15
});
```

### 地図操作

地図の下に次のボタンを置いてください。

1. 「現在地へ移動」
2. 「全避難所を表示」

ボタン仕様：

- 高さ最低 `44px`
- 文字サイズ `15px`
- 太字
- 枠線 `1px solid #b8c2ce`
- 角丸 `6px`
- 背景 `#ffffff`
- 文字色 `#2f5baa`
- hover/focus 背景 `#e4edf8`

「現在地へ移動」：

- `navigator.geolocation` を使用
- 成功時は現在地へ移動
- ズームは `15` 程度
- 現在地には青色の円形マーカーを表示
- 既に現在地マーカーがある場合は再利用して位置だけ更新
- 取得失敗時は地図直下の専用メッセージ欄にエラーを表示
- 報告フォームのメッセージ欄を流用しない

「全避難所を表示」：

- すべての座標付き避難所が表示される範囲へ戻す
- 避難所が 0 件の場合は何もせず、JavaScript エラーを発生させない

### 凡例

地図の下に凡例を表示してください。

- 受け入れ可・空き：緑
- やや混雑：黄色
- 混雑：赤
- 受け入れ不可：赤

凡例の文字サイズは `14px`、色丸は `20px × 20px` 程度にしてください。

地図メッセージ欄は次の仕様にしてください。

- 最小高さ：`20px`
- 文字サイズ：`13px`
- エラー文字色：`#c0392b`
- `role="status"`
- `aria-live="polite"`

---

## 7. 災害情報報告フォーム

マップの右側にカードとして表示し、スマートフォンでは最後に表示してください。

見出し：

- 災害の情報を報告する

説明：

- 身近で起きている災害の状況を入力してください。

入力項目：

1. 災害の種類
2. 災害の種類の詳細（「その他」選択時のみ表示）
3. 位置情報
4. 状況の説明
5. 報告を送信するボタン

災害の種類の選択肢：

- 地震
- 洪水・浸水
- 土砂災害
- 火災
- 強風・突風
- その他

動作：

- 「その他」を選ぶと詳細入力欄を表示
- 「その他」以外では詳細入力欄を隠し、値をクリア
- 「その他」を選んだ場合は詳細入力を必須にする
- 現在地取得ボタンでブラウザの位置情報を取得
- 取得した緯度・経度を位置情報欄へ入力
- 送信中、成功、失敗の状態をメッセージ欄に表示
- 成功時はフォームをリセット
- 送信先は既存の災害報告 API を使用

現在地取得ボタンの表示仕様：

- ブラウザが Geolocation API に対応していない場合は「現在地を取得できません。位置情報を入力してください。」を表示
- 取得中は「現在地を取得しています...」を表示
- 成功時は「現在地を入力しました。」を表示
- 取得失敗時は「現在地を取得できません。位置情報を入力してください。」を表示
- これらのメッセージは報告フォームのメッセージ欄に表示し、成功時は成功色、失敗時はエラー色にする
- `navigator.geolocation.getCurrentPosition` の成功値を小数点以下6桁程度で緯度・経度文字列にして位置情報欄へ入れる

フォームの CSS：

- フォーム間隔：`18px`
- ラベル：`16px`、太字、`#435466`
- 入力欄・select・textarea：`16px`
- 枠線：`1px solid #b8c2ce`
- 角丸：`7px`
- padding：上下 `13px`、左右 `14px`
- textarea 最小高さ：`120px`
- 送信ボタン高さ：最低 `50px`
- 送信ボタン最大幅：`260px`
- 送信ボタン文字サイズ：`18px`
- 成功表示：`#239b4b`
- エラー表示：`#c0392b`

スマートフォンでは位置情報欄と現在地取得ボタンを縦並びにしてください。

送信 API の最低仕様：

```http
POST /api/disaster_reports
Content-Type: application/json
```

```json
{
  "disaster_type": "地震",
  "other_detail": "",
  "location": "青森市中央1-1-1",
  "situation": "建物に被害があります"
}
```

成功時は現在の言語に合わせた成功メッセージを返してください。

---

## 8. 気象警報・注意報

見出し：

- 青森市 気象警報・注意報

見出しの右側に次を表示してください。

- 10分おき自動更新
- 更新アイコン付き
- 色は `#239b4b`
- 文字サイズはデスクトップ `16px`、スマートフォン `13px`

初期表示は「読み込み中...」としてください。

既存の気象警報 API を使用し、次を表示してください。

- 対象地域
- 警報・注意報の一覧
- データ取得時刻
- 気象庁発表時刻

API のレスポンスは少なくとも次の形を扱えるようにしてください。

```json
{
  "area_name": "青森市",
  "warnings": [
    {"name": "大雨警報", "code": "03", "status": "発表"}
  ],
  "report_time": "2026年09月10日 10:00",
  "last_fetch_time": "2026年09月10日 10:01",
  "error": false
}
```

`error` が `true` の場合は警報一覧を無理に表示せず、失敗メッセージを表示してください。地域名も選択言語に合わせて表示してください。

動作：

- 初回表示時に取得
- `600000ms` ごとに自動更新
- 通信失敗時は失敗メッセージを表示
- 警報がない場合はチェックアイコン付きで「現在、警報・注意報は発表されていません」と表示
- 警報がある場合はリスト表示

警報リストの各項目は、警報名と状態（`発表` または `継続`）を表示してください。警報名と状態を組み立てる際は、ユーザー入力を HTML として解釈せず、安全にテキストとして扱ってください。外部 API が失敗した場合でも、画面全体を壊さず、パネル内にエラーを表示してください。

パネル仕様：

- 背景：`#f7f8fa`
- 角丸：`11px`
- padding：上 `20px`、左右 `18px`、下 `17px`
- 通常本文：`15px`
- 地域名・警報なし表示：`17px`、太字

---

## 9. 指示・発信ボード

見出し：

- 指示・発信ボード

見出し直下に次の補足を表示してください。

- 防災対策課が確認した情報のみ表示しています
- シールドアイコン付き
- 背景：`#eaf2fb`
- 文字色：`#435466`
- 文字サイズ：`14px`
- padding：上下 `8px`、左右 `12px`
- 角丸：`6px`

住民向け情報がある場合：

- 最新の情報を上に表示
- 内容
- 最終更新日時
- 避難先（存在する場合）
- 状態（存在する場合）

情報がない場合：

- 「現在、住民向けの指示・発信はありません。」と表示
- 文字色：`#607286`
- 文字サイズ：`15px`

各通知カード：

- 背景：`#f7f8fa`
- 左ボーダー：`5px solid #2f5baa`
- 角丸：`7px`
- padding：上下 `15px`、左右 `16px`
- 通知内容：`16px`、太字、行間 `1.6`
- 日時：`13px`
- 通知間隔：`12px`

スマートフォンでは通知見出し部分を縦並びにし、日時が内容と重ならないようにしてください。

---

## 10. 日英翻訳

ホーム画面の固定文言は日本語と英語の両方を用意してください。

### 日本語

```text
防災アプリ
ホーム
避難所検索
避難所登録
指示・発信
ログイン
ログアウト
ログイン中
言語選択
目次
避難所マップ
現在地へ移動
全避難所を表示
凡例
受け入れ可・空き
やや混雑
混雑
受け入れ不可
現在地を取得できませんでした。
災害の情報を報告する
身近で起きている災害の状況を入力してください。
災害の種類
災害の種類を選択してください
地震
洪水・浸水
土砂災害
火災
強風・突風
その他
災害の種類の詳細
災害の種類を入力してください
位置情報
住所や場所を入力してください
現在地を取得
状況の説明
被害の状況や周囲の様子を入力してください
報告を送信する
青森市 気象警報・注意報
10分おき自動更新
読み込み中...
指示・発信ボード
防災対策課が確認した情報のみ表示しています
最終更新
避難先
状態
現在、住民向けの指示・発信はありません。
データ取得
気象庁発表
気象情報の取得に失敗しました
現在、警報・注意報は発表されていません
```

### English

```text
Disaster Prevention App
Home
Shelter Search
Register Shelter
Announcements
Log in
Log out
Logged in
Language
Contents
Shelter Map
Go to my location
Show all shelters
Legend
Available
Somewhat crowded
Crowded
Not accepting
Could not get your location.
Report a Disaster
Enter information about a disaster in your area.
Disaster type
Select a disaster type
Earthquake
Flooding
Landslide
Fire
Strong wind
Other
Disaster type details
Enter the disaster type
Location
Enter an address or place
Get current location
Situation description
Describe the damage and surroundings
Send report
Aomori City Weather Warnings
Refreshes every 10 minutes
Loading...
Announcements
Only information confirmed by the disaster management office is shown.
Last updated
Shelter
Status
There are no announcements for residents.
Data retrieved
JMA announcement
Failed to retrieve weather information.
There are currently no active warnings or advisories.
```

### 動的メッセージの英語

英語を選択している場合、次の動的メッセージも英語で表示してください。

```text
Could not get your location. Please enter it manually.
Getting your location...
Current location entered.
Your report has been received.
Failed to send. Please try again later.
Please enter the disaster type, location, and situation.
Please enter details for the disaster type.
Failed to retrieve weather information.
```

サーバーから返る API メッセージも現在のセッション言語に合わせてください。ユーザーが入力した災害内容、場所、通知内容は翻訳せず原文を表示してください。

気象警報名を英語表示する場合は、警報コードをキーにした翻訳辞書を用意してください。最低限、次の分類を対応させてください。

- 大雨：Heavy rain
- 洪水：Flood
- 暴風：Storm
- 大雪：Heavy snow
- 暴風雪：Snowstorm
- 波浪：High waves
- 高潮：Storm surge
- 雷：Thunderstorm
- 強風：Strong wind
- 濃霧：Dense fog
- 乾燥：Dry conditions
- なだれ：Avalanche
- 低温：Low temperatures
- 霜：Frost
- 着氷・着雪：Icing / snow accretion
- 土砂災害：Landslide

警報の状態は次のように表示してください。

- 発表：Issued
- 継続：Continued

避難所データの値が日本語で保存されている場合は、ポップアップ表示時だけ次のように翻訳してください。

```text
受け入れ可   -> Accepting
受け入れ不可 -> Not accepting
空き         -> Available
やや混雑     -> Somewhat crowded
混雑         -> Crowded
```

避難所名、住所、通知内容などのユーザー登録データは勝手に翻訳せず、そのまま表示してください。

---

## 11. アクセシビリティ

必ず次を実装してください。

- フォームの全入力欄に label を付ける
- ボタンは `button` 要素を使用する
- アイコンには `aria-hidden="true"` を付ける
- 地図エラーメッセージは `role="status"` と `aria-live="polite"`
- 色だけで避難所状態を判断させず、凡例と文字でも表示
- キーボード操作時に `:focus-visible` が分かる状態にする
- タップ領域は最低 `44px`
- スマートフォンで文字やボタンが横にはみ出さないようにする
- 目次リンクの遷移先が固定ヘッダーに隠れないようにする

---

## 12. 既存機能との接続条件

既存システムに同等機能がある場合は、それを再利用してください。

最低限、次のデータと API を接続してください。

- `shelters`：避難所配列
- ホームルートのテンプレートへ、避難所配列を `shelters` として渡す
- ホームルートのテンプレートへ、住民向けの指示・発信を `resident_notices` として渡す
- `resident_notices` は更新日時または作成日時の新しい順に並べる
- 災害報告 API：`POST /api/disaster_reports`
- 気象警報 API：`GET /api/weather_warnings`
- 言語変更処理：`POST /set_language`
- セッションの言語キー：`language`

避難所登録側では、住所をジオコーディングして次の値を保存してください。

- `address`
- `latitude`
- `longitude`
- `accepting`
- `congestion`

住所のジオコーディングに OpenStreetMap Nominatim を使う場合は、次の条件を守ってください。

- 検索 URL は `https://nominatim.openstreetmap.org/search`
- `format=jsonv2`
- `limit=1`
- 明示的な User-Agent を付ける
- タイムアウトを設定する
- 結果がない場合や通信失敗時は登録を完了扱いにしない
- Nominatim の利用ポリシーに従い、登録操作時など必要な場合だけ呼び出す

Nominatim はホーム画面の表示時に呼び出さないでください。ホーム画面は保存済みの `latitude` と `longitude` を使って地図を描画してください。既存の避難所データに座標がない場合は、そのデータを削除せず、地図マーカーだけを表示対象外にしてください。

言語変更フォームの現在ページ保持には、現在のパスとクエリを `next` などの hidden 値で渡してください。リダイレクト先は同一ホストの安全な URL のみ許可し、外部 URL へリダイレクトしないでください。

---

## 13. 完了条件

次をすべて満たしたら完了としてください。

- ホーム画面がデスクトップとスマートフォンで崩れない
- 目次が画面上部に表示される
- 目次からマップ、報告フォーム、警報、発信へ移動できる
- 避難所マーカーが住所から得た座標に表示される
- 避難所状態に応じてマーカーが緑・黄・赤で表示される
- マーカーのクリックで避難所名、住所、受け入れ可否、混雑度が表示される
- 「現在地へ移動」が動作する
- 「全避難所を表示」が動作する
- 位置情報取得失敗時に地図直下へエラーが表示される
- 災害報告フォームが送信できる
- 「その他」の詳細入力が正しく表示・非表示になる
- 気象警報が初回表示され、10分ごとに更新される
- 指示・発信ボードが表示される
- 日本語と英語を切り替えられる
- 画面遷移後も言語選択が維持される
- 入力欄、ボタン、目次がスマートフォンで押しやすい
- JavaScript エラーがない
- Leaflet、OpenStreetMap タイル、Font Awesome の外部リソースが読み込めない場合でも、フォームや他の情報が全画面エラーにならない
- Python またはサーバー側の構文エラーがない
- 既存の他機能を壊していない

検証では少なくとも次を確認してください。

- 未ログインでホーム画面を開ける
- 日本語と英語の切り替え後、ホーム画面を再表示して選択状態が維持される
- 座標付き避難所がマーカーになる
- 座標なし避難所が JavaScript エラーを起こさない
- 全避難所表示ボタンが避難所 0 件でもエラーにならない
- Geolocation API が失敗した場合に地図側と報告フォーム側で適切なメッセージが表示される
- 災害報告の必須項目不足と送信成功を確認する
- 気象 API の成功、警報なし、通信失敗の 3 ケースを確認する
- 通知が 0 件と複数件の両方を確認する
- デスクトップ幅と `767px` 以下のスマートフォン幅で横方向のはみ出しがない

最後に、実装したファイル、利用した API、実行したテスト、未解決の前提条件を簡潔に報告してください。